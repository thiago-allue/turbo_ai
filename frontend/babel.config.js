/**
 * Babel configuration for the Next.js project.
 * Transpiles modern JavaScript (and TypeScript) features to browser-compatible versions.
 */

module.exports = {
  presets: [
    '@babel/preset-env',
    'next/babel'
  ],
};
